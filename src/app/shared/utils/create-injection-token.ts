// export function createInjectionToken<TFactoryFn extends (...args: any[]) => any>(
// description: string,
// factoryFn: TFactoryFn,
// { isRoot = false }: { isRoot?: boolean } = {}
// ): [InjectionToken<ReturnType<TFactoryFn>>, TFactoryFn] {
// if (isRoot) {

// }

// return [
// new InjectionToken(description),

// ]
// }
